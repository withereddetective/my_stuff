public class bruh {
    public static void main(String[] args){
    
        int x = 10;
        int y = 4;
        double z = (double) x / (double) y;
        System.out.println(z);

        x = 15;
        y = 2;
        z = (double) x / (double) y;
        System.out.println(z);

        // i would use byte for person age
        // i would use double for bank account balance
        // i would use int for number of days since year 0

        x -= 10;
        int total = 10;
        total *= 4;
        int count = 8;
        count = count / 2;

        // the output for n is 4.0

        int points = 50;
        points += 20;
        points -= 5;
        points *= 2;
        points /= 5;
        System.out.println(points);
    }
}
